{
  "name": "bottom-terminal",
  "displayName": "Bottom Terminal",
  "keywords": ["bottom", "terminal", "bottom-terminal", "bottom terminal", "status bar terminal", "status bar", "footer terminal"],
  "description": "Terminal toggle on the bottom status bar. So comfortable!",
  "version": "0.0.1",
  "icon": "icon.jpeg",
  "engines": {
    "vscode": "^1.70.0"
  },
  "publisher": "fuadpashayev",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [{
      "command": "bottom-terminal",
      "title": "Bottom Terminal"
    }]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.70.0",
    "@types/glob": "^7.2.0",
    "@types/mocha": "^9.1.1",
    "@types/node": "16.x",
    "eslint": "^8.20.0",
    "glob": "^8.0.3",
    "mocha": "^10.0.0",
    "typescript": "^4.7.4",
    "@vscode/test-electron": "^2.1.5"
  }
}
